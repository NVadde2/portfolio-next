---
title: "Swimming (placeholder — replace me)"
date: "2026-08-18"
description: "Placeholder write-up. Replace this file's content with your own."
---

**This is a placeholder file, not real content.** It exists so the `/hobbies/[slug]` route has at least one page to statically export — required by Next.js since this site uses `output: "export"`.

To turn this into your real write-up:

1. Replace everything in this file (including the frontmatter above) with your own writing about swimming.
2. Or rename it and write about a different hobby instead — any file dropped into `content/hobbies/<slug>.md` where `<slug>` matches a slug in `src/data/hobbies.ts` will automatically get a "Read more" link on that hobby's tile.
3. Delete this file once you no longer need a placeholder — as long as at least one `content/hobbies/*.md` file exists, the build stays happy.
